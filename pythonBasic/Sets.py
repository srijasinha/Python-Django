# sets - duplicate elements are not allowed
# it is unordered, * Note: Set items are unchangeable, but you can remove items and add new items.
# no Indexing is used in set ,it is defined random
# use for loop for iteration
a = {1, 2, 3, 2, 4, 1}
print(a[0])
